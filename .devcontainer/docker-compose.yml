version: '3'

services:
  app:
    image: ghcr.io/pzd24/devcontainer:master
    platform: linux/amd64
    container_name: dev
    volumes:
      - ..:/home/vscode/workspace
      - /data0:/data0
    ports:
      - "8080:8080"
    command: sleep infinity
